#!/bin/sh
set -eu
if [ "$(/usr/bin/id -u)" -ne 0 ]; then
    printf '%s\n' '{"ok":false,"firstMissingSignal":"xenos-run-root-required"}'
    exit 1
fi
exec /usr/bin/python3 /usr/local/sbin/agathodaimon/xenos/_launcher.py "$@"
