when:
  - event: push
    branch: main

steps:
  - name: release-publish
    image: python:3.12-alpine
    environment:
      FORGEJO_TOKEN:
        from_secret: FORGEJO_TOKEN
    commands:
      - python3 ci/release_publish.py
