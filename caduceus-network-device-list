#!/bin/sh
set -eu
PYTHON=${CADUCEUS_STAFF_PYTHON:-/var/lib/caduceus/venv/bin/python3}
[ -x "$PYTHON" ] || PYTHON=python3
STAFF_SHELF=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
export PYTHONPATH="$STAFF_SHELF"
[ "$#" -eq 1 ] && [ "$1" = list ] || { echo "usage: caduceus-network-device-list list" >&2; exit 2; }
exec "$PYTHON" -m agathodaimon.network.identity device-list
